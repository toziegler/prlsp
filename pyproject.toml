[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "prlsp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pygls>=2", "lsprotocol"]

[tool.setuptools.packages.find]
include = ["prlsp*"]
